id: gnfac_advisory_region
migration_group: gnfac
migration_tags: {}
label: 'GNFAC Advisory Region'

source:
  plugin: gnfac_advisory_region

destination:
  plugin: 'entity:taxonomy_term'

process:
  name: name
  vid:
    plugin: default_value
    default_value: advisory_region

migration_dependencies: {}

dependencies:
  module:
    - gnfac_migrate